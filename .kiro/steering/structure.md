# Project Structure

The repository is freshly initialized with no source code yet.

```
.
├── .gitignore        # Node.js / modern JS framework ignores
├── .kiro/            # Kiro AI assistant configuration
│   └── steering/     # Steering rules for AI context
├── .vscode/          # VS Code workspace settings
├── LICENSE           # MIT License (Insight Genesis, 2026)
└── README.md         # Project README
```

## Notes
- The project has not been scaffolded yet. Structure will evolve once a framework is chosen and initialized.
- `node_modules/`, build outputs (`.next`, `dist`, `out`, `.output`), and env files are gitignored.
