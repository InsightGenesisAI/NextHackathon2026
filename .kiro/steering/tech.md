# Tech Stack

## Platform
- Node.js (JavaScript/TypeScript)

## Anticipated Frameworks & Tools
Based on the `.gitignore`, the project is configured to support:
- Next.js, Nuxt, Vite, or similar modern JS framework
- TypeScript
- Package managers: npm, yarn, or pnpm

## Commands
No `package.json` exists yet. Once the project is scaffolded, common commands will likely include:
- `npm install` / `yarn` / `pnpm install` — install dependencies
- `npm run dev` — start dev server
- `npm run build` — production build
- `npm test` — run tests

## Environment
- Environment variables via `.env` files (`.env` is gitignored; use `.env.example` for templates)
